const START = "START";
const RESULT = "RESULT";
const TASK = "TASK";
const HTTP = "HTTP";
const WORK_FLOW = "WORK_FLOW";
const AND = "AND";
const OR = "OR";

export {
  START,
  RESULT,
  TASK,
  HTTP,
  WORK_FLOW,
  AND,
  OR
}
